A makefile is only ever used a simple helper for developers to use for building testing or linting. 
It never becomes a carrier of big logic.

If it comes to a case where the Makefile start to get large logic, stop, question scope creep and verify the architecture and ask questions before continuing.
